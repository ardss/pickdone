{
  "name": "pickdone-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "拾事 —— 本地优先的待办与番茄钟桌面应用（Electron + Vue + SQLite），内置 AI/CLI 管理接口",
  "scripts": {
    "check:fast": "npm run check:fast --prefix pickdone",
    "check:all": "npm run check:all --prefix pickdone",
    "test": "npm test --prefix pickdone"
  }
}
